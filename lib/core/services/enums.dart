 enum UserType
{
  admin,
  client
  
}
enum ItemStatus{
  available,
  notAvailable,
  willBeAvailable
}
enum DeliveryType{
  inPlace,
  shipped,
}
enum OrderStatus{
  initial,
  inProgress,
  delivered,
  cancelled
}